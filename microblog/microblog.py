# This is a Python script at the top-level that defines the Flask application instance.

# bashrc => bash run commands

from app import app
# useage of both app_entities
# The Flask application instance is called app and is a member of the app package
# The from app import app statement imports the app variable that is a member of the app package


# Starting with version 1.0, Flask allows you to register environment variables that you want to be
# automatically imported when you run the flask command.